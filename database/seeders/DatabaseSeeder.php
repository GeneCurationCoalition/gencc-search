<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     *
     * Note: gencc-search is a read-only front-end that uses gencc-sub's database.
     * Data seeding is handled by gencc-sub.
     *
     * @return void
     */
    public function run()
    {
        // No seeders needed - using gencc-sub database
    }
}
